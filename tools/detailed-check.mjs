import {chromium} from '@playwright/test';import fs from 'node:fs';fs.mkdirSync('artifacts/detailed',{recursive:true});const b=await chromium.launch({headless:true,channel:'msedge'}),p=await b.newPage({viewport:{width:1440,height:900}});const errors=[];p.on('pageerror',e=>errors.push(e.message));p.on('console',m=>{if(m.type()==='warning')console.log('WARN',m.text())});await p.goto(process.env.TEST_URL||'http://127.0.0.1:5173/');await p.waitForFunction(()=>window.__velocity,null,{timeout:120000});for(const key of ['p911','m3','gt40']){while(await p.evaluate(()=>window.__velocity.snapshot().car)!==key)await p.click('#nextCar');await p.waitForFunction(()=>window.__velocity.snapshot().assetStatus==='ready',null,{timeout:90000});const snap=await p.evaluate(()=>window.__velocity.snapshot());if(snap.assetError||snap.modelKind!=='imported-glb'||snap.wheelCount!==4||!snap.assetCredit)throw Error('Detailed model missing: '+key);if(snap.wheelPositions.flat().some(x=>!Number.isFinite(x))||Math.abs(snap.wheelPositions[0][2]-snap.wheelPositions[1][2])>.02)throw Error('Invalid front axle: '+key);console.log(key,snap.dimensions,snap.wheelPositions);await p.click('#vehicleBtn');await p.click('[data-view=front]');await p.waitForTimeout(500);await p.screenshot({path:`artifacts/detailed/${key}-front.png`});await p.click('[data-view="side"]');await p.waitForTimeout(500);await p.screenshot({path:`artifacts/detailed/${key}-side.png`});await p.click('#vehicleBack');await p.click('#startBtn');await p.waitForFunction(()=>window.__velocity.snapshot().state==='race');for(const mode of ['cockpit','bonnet']){await p.keyboard.press('KeyC');await p.waitForTimeout(300);await p.screenshot({path:`artifacts/detailed/${key}-${mode}.png`});}await p.keyboard.press('Escape');await p.click('#quitBtn');}await b.close();if(errors.length)throw Error(errors.join('\n'));console.log('PASS: three detailed models, wheel rigs and cameras');
